# AGENTS.md

本文件是本项目的行为准则，任何 AI agent 或开发者在本仓库内工作时都必须遵守。

## 工作约定

1. 每次改动完成后，都必须创建一次对应的 git commit，以便后续追踪和回滚。
2. 每次改动后，都必须编写或更新相关测试，并在交付给用户之前，确保所有测试和验证全部通过。
3. 每次修改完毕后，必须自行完整测试一遍，确认没有问题之后再交付给客户。

## 说明

- 规则 1 要求仓库处于 git 管理之下；若仓库尚未初始化，应先执行 `git init` 并建立首个基线提交。
- 规则 2 中的"全部通过"以项目实际配置的测试与验证命令为准（例如 lint、类型检查、单元测试、构建），未通过不得交付。
- 规则 3 要求交付前跑一次完整验证链路，而不是只跑改动涉及的那几个用例：
  - 执行 `pnpm verify`（类型检查 + 全部测试），必须是全绿。
  - 启动应用后执行 `pnpm smoke`，用真实 API 客户端打真实服务端，覆盖增 / 查 / 删全流程。
  - 涉及界面改动时执行 `pnpm verify:ui`（无头浏览器真实点击），或亲眼看一眼渲染结果，
    不要只依赖测试断言。
- 规则 3 的由来（一次真实事故）：前端给没有 body 的 `DELETE` 请求加上了
  `Content-Type: application/json`，Fastify 的 JSON 解析器以
  `FST_ERR_CTP_EMPTY_JSON_BODY` 直接拒绝，删除信息卡功能完全不可用。
  单元测试全绿却漏掉了它——因为测试断言的是 mock 收到的参数，而 mock 不校验请求头。
  **教训：mock 无法替代真实链路，涉及协议细节（请求头、状态码、序列化）的改动
  必须有针对真实服务端的契约测试或手工实测。**
